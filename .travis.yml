language: python

python:
  - "2.7"
  - "3.3"

# command to install dependencies
install:
  - sudo apt-get install -qq libhdf5-serial-dev 
  - "pip install cython==0.20.2"
  - "pip install numpy"
  - "pip install numexpr"
  - "pip install tables"

script: 
  - python numper.py
